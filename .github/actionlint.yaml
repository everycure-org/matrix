# actionlint.yaml is a configuration file for the actionlint linter. 
# We define a self-hosted-runner block listing allowed self-hosted runner labels is required to avoid actionlint complaining about unknown labels.
self-hosted-runner:
  # Labels of self-hosted runner in array of strings.
  labels:
    - gha-runner-scale-set