name: distribute-release-job

on:
  repository_dispatch:
    types: distribute-release
jobs:
  build:
    name: Distribute the release
    runs-on: ubuntu-latest
    steps:
      - name: Sleep for 3 seconds
        run: sleep 3s
        shell: bash
      - name: echo-success
        run: echo "Release distributed successfully"