# NOTE: This docker-compose.yml was partially generated using AI assistance.

version: '3.8'

services:
  kg-dashboard:
    build: .
    ports:
      - "8080:80"
    container_name: kg-dashboard
    restart: unless-stopped 