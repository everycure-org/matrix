run_name: ${oc.env:RUN_NAME}

gcs_bucket: gs://${oc.env:GCP_BUCKET}
gcp_project: ${oc.env:GCP_PROJECT_ID}

# NOTE: MLflow does not like "new options" in the mlflow.yml
# due to schema validation. Will make PR.
mlflow_artifact_root: ${gcs_bucket}/runs/${run_name}/mlflow

paths:
  raw: ${base.paths.raw}

  # Release-based
  ingestion: ${gcs_bucket}/${base.paths.ingestion}
  integration: ${gcs_bucket}/${base.paths.integration}
  embeddings: ${gcs_bucket}/${base.paths.embeddings}

  # Run-based
  modelling: ${gcs_bucket}/${base.paths.modelling}
  evaluation: ${gcs_bucket}/${base.paths.evaluation}
  matrix_generation: ${gcs_bucket}/${base.paths.matrix_generation}
  inference: ${gcs_bucket}/${base.paths.inference}

  tmp: ${gcs_bucket}/${base.paths.tmp}
