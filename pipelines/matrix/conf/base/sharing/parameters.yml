sharing.reporting.experiment:
  RUN_NAME: &run_name ${globals:run_name}
  EXPERIMENT_NAME: &experiment_name ${globals:run_name}
  GIT_SHA: &git_sha ${globals:git_sha}
  RELEASE_VERSION: &release_version ${globals:versions.release}
  DATA: ${globals:data_sources}
  MODEL: xg_ensemble
  DESCRIPTION: 'experiment_description'
  DOCUMENT: 'experiment_status'
  TIER: 'experiment_tier'
