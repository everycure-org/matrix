matrix_transformations:
  transformations:
    no_transformation:
      _object: matrix.pipelines.matrix_transformations.transformations.NoTransformation

    # Frequent Flyer excluded until passes T3 criteria
    # frequent_flyer:
    #   _object: matrix.pipelines.matrix_transformations.transformations.AlmostPureRankBasedFrequentFlyerTransformation
    #   decay: 0.05

    # uniform_frequent_flyer:
    #   _object: matrix.pipelines.matrix_transformations.transformations.UniformRankBasedFrequentFlyerTransformation
    #   decay: 0.05

  score_col: "treat score"