# NOTE: Do not put actual credentials in here, extract them 
# from the environment instead using the omegaconf loader.
neo4j_credentials:
  authentication.type: basic
  authentication.basic.username: ${oc.env:NEO4J_USER,neo4j}
  authentication.basic.password: ${oc.env:NEO4J_PASSWORD,admin}