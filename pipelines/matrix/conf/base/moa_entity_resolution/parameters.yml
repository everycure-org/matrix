moa_entity_resolution:
  prenormalize_func:
    # Use ARAX node synonymizer for prenormalization
    object: matrix.pipelines.modelling.utils.partial_
    func:
      object: matrix.pipelines.moa_entity_resolution.prenormalize_drugmechdb.prenormalize_drugmechdb_entities_arax
    synonymizer_endpoint: http://localhost:8081

    # # Use RENCI name resolver for prenormalization
    # object: matrix.pipelines.modelling.utils.partial_
    # func:
    #   object: matrix.pipelines.moa_entity_resolution.prenormalize_drugmechdb.prenormalize_drugmechdb_entities_renci
    # name_resolver: https://name-resolution-sri-dev.apps.renci.org
    # timeout: 5 # (seconds) Time to wait for the name resolver to respond