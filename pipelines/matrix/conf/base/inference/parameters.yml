inference.matrix_generation_options:
  batch_by: # Column to batch over when making predictions
    - target 
  n_reporting: 100000 # Number of items in the final report; not applicable in request pipeline
inference.score_col_name: 'treat score'