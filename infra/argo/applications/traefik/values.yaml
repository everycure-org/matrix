
external-dns:
  provider:
    name: google

traefik:
  service:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: '*.platform.dev.everycure.org'
  
  autoscaling:
    enabled: true
    maxReplicas: 3

  # TODO: add affinity rules
  # affinity:
  ingressRoute:
    dashboard:
      enabled: true
  
  logs:
    general:
      level: ERROR
    access:
      enabled: true