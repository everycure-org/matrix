# NOTE: This file was partially generated using AI assistance.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: "neo4j-licenses"
spec:
  refreshInterval: "24h"  # License changes less frequently than passwords
  secretStoreRef:
    name: gcp-store
    kind: ClusterSecretStore
  target:
    name: neo4j-licenses
    creationPolicy: 'Owner'
    template:
      type: Opaque
      data:
        bloom.license: "{{ `{{ .bloom_license }}` }}"

  data:
    - secretKey: bloom_license
      remoteRef:
        key: neo4j_bloom_server_key
