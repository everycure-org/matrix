apiVersion: v1
kind: Service
metadata:
  name: bte-trapi
  labels:
    app: bte-trapi
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      nodePort: {{ .Values.service.nodePort }}
  selector:
    app: bte-trapi