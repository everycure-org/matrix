# infra/argo/applications/bte-trapi/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bte-trapi
  labels:
    app: bte-trapi
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: bte-trapi
  template:
    metadata:
      labels:
        app: bte-trapi
    spec:
      containers:
      - name: bte-trapi
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        {{- range .Values.env }}
          - name: {{ .name }}
            value: {{ .value | quote }}
        {{- end }}
        resources:
          limits:
            cpu: {{ .Values.resources.limits.cpu }}
            memory: {{ .Values.resources.limits.memory }}
          requests:
            cpu: {{ .Values.resources.requests.cpu }}
            memory: {{ .Values.resources.requests.memory }}
