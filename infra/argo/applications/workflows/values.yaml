argo-workflows:
  # baseHref: /argo-workflows/
  server: 
    extraArgs:
      - --auth-mode=server

  artifactRepository:
    archiveLogs: true
    gcs:
      bucket: dummy
      keyFormat: "{{ \"{{workflow.namespace}}/{{workflow.name}}/{{pod.name}}\" }}"
  
  controller:
    metricsConfig:
      enabled: true
