apiVersion: v1
kind: Service
metadata:
  name: kg-dashboard
  labels:
    app: kg-dashboard
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      nodePort: {{ .Values.service.nodePort }}
  selector:
    app: kg-dashboard