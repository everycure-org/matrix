# NOTE: This file was partially generated using AI assistance.
# Test workflow to verify NATS max payload configuration
# This workflow generates a large payload (~5MB) to test if the EventSource
# can successfully publish it to NATS after our configuration changes.
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: test-large-payload-
  namespace: argo-workflows
  labels:
    trigger_release: "True"
    workflows.argoproj.io/phase: "Succeeded"
    release_version: "test-payload-5mb"
    git_sha: "test123abc"
spec:
  entrypoint: dummy-success
  templates:
    - name: dummy-success
      container:
        image: alpine:latest
        command: [sh, -c]
        args: ["echo 'Test workflow for large payload'; exit 0"]
    
    # Add a massive stored template to simulate large payload
    # This template contains ~5MB of dummy data in annotations/comments
    - name: large-template-1
      container:
        image: alpine:latest
        command: [sh, -c]
        args:
          - |
            # Dummy data to increase payload size - Part 1
            # START_PADDING_DATA_1
