apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "9"
spec:
  destination:
    namespace: redis
    server: {{ .Values.spec.destination.server }}
  project: default
  source:
    chart: redis
    repoURL: https://ot-container-kit.github.io/helm-charts
    targetRevision: 0.16.5
    helm:
      values: |
        storageSpec:
          volumeClaimTemplate:
            spec:
              resources:
                requests:
                  storage: 100Gi
        externalConfig:
          enabled: true
          data: |
            # Disable RDB snapshotting entirely (ephemeral cache only)
            save ""
            # Disable AOF persistence
            appendonly no
            # Disable stop-writes-on-bgsave-error since we're not persisting
            stop-writes-on-bgsave-error no
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      allowEmpty: true
